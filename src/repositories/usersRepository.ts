import {SortingPaginationProcessed} from "../libs/types/commonTypes";

const usersRepository = {
    async getUsers(sortingPaginationProcessed: SortingPaginationProcessed) {

    }
}
export default usersRepository;